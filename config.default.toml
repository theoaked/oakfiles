[server]
host = "0.0.0.0"
port = 8080
session_timeout_minutes = 30

[mdns]
enabled = true
hostname = ""   # empty = use OS hostname

[paths]
roots = []      # fill with absolute paths, e.g. ["D:\\Files", "E:\\Media"]
zip_download_enabled = true
zip_max_size_mb = 2048

[security]
bcrypt_cost = 12
